<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.pikapika.app.mapper.BbsMapper" >
    <resultMap id="BaseResultMap" type="BbsEntity" >
        <id column="bbs_id" property="bbsId" jdbcType="INTEGER" />
        <result column="bbs_title" property="bbsTitle" jdbcType="VARCHAR" />
        <result column="bbs_content" property="bbsContent" jdbcType="VARCHAR" />
        <result column="uaccount_id" property="uaccountId" jdbcType="VARCHAR" />
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
    </resultMap>

    <sql id="Base_Column_List" >
        bbs_id, bbs_title, bbs_content, uaccount_id, create_time
    </sql>

    <select id="getAll" resultMap="BaseResultMap"  >
       SELECT 
       <include refid="Base_Column_List" />
       FROM bbs
    </select>
    
    <select id="getBbsById" resultMap="BaseResultMap" parameterType=""  >
       SELECT 
       <include refid="Base_Column_List" />
       FROM bbs WHERE bbs_id = #{bbsId}
    </select>
    
    <!-- <insert id="insertBbs" parameterType="menu" keyProperty="id" useGeneratedKeys="true">
	   insert into webdesk_menu
		(systemCode,menuName,MENUURL,SUPERMENUID,MENUTYPE,SORT,MENUICON,ISHOMEPAGE,ISUSE,DR,CREATETIME)
		values (#{systemCode},#{menuName},#{menuUrl},#{superMenuID},#{menuType},#{sort},#{menuIcon},#{isHomePage},#{isUse},#{dr},#{createTime})
		
	</insert>  -->

</mapper>